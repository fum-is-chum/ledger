name: ledger_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  bloc: ^8.1.0
  flutter_bloc: ^8.1.1
  bloc_concurrency: ^0.2.0
  equatable: ^2.0.5
  collection: ^1.16.0
  convert: ^3.1.1
  permission_handler: ^10.2.0
  ledger: ^1.1.0
  ledger_ethereum: ^1.0.2
  web3dart_avacus: ^2.4.0
  eth_sig_util: ^0.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
