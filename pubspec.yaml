name: ledger_scallop
description: A Flutter plugin to scan, connect & sign transactions using your Ledger devices
version: 1.0.0
publish_to: none
homepage: https://developers.ledger.com/
repository: https://github.com/fum-is-chum/ledger
issue_tracker: https://github.com/fum-is-chum/ledger/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  scallop_flutter_reactive_ble: ^5.4.1
  protobuf: ^6.0.0
  ledger_usb: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
