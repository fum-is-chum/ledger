name: ledger
description: A Flutter plugin to scan, connect & sign transactions using your Ledger devices
version: 1.1.0
homepage: https://developers.ledger.com/
repository: https://github.com/wakumo/ledger
issue_tracker: https://github.com/wakumo/ledger/issues

environment:
  sdk: '>=2.17.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_reactive_ble: ^5.0.3
  ledger_usb: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
